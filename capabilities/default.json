{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the two local application windows to use Tauri core APIs.",
  "windows": ["main", "floating-ball"],
  "permissions": ["core:default"]
}
